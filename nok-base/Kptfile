apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: nok-base
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: base settings for nok
pipeline:
  mutators:
    - image: gcr.io/kpt-fn/set-namespace:v0.4.1
      configMap:
        namespace: nok-base
