Needs to be install separately via `kubectl apply`, do not use kpt

`install.yaml` is obtained via `https://github.com/gnmic/operator/releases/download/latest/install.yaml`
and then replace all instances of `gnmic-system` by `nok-base`