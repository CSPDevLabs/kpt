apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: nok-observability
  annotations:
    config.kubernetes.io/local-config: "true"
  namespace: nok-base
info:
  description: observability apps for nok
pipeline:
  mutators:
    - image: gcr.io/kpt-fn/set-namespace:v0.1
      configMap:
        namespace: nok-base
